@using RPGCharacterManager.Models.OscarsDatabaseTestingModels
@model IEnumerable<TestingModel>
@{
    Layout = "~/Views/_Layout.cshtml";
}
<!DOCTYPE html>
<html>

<body>


<div>
    @if ( @Model != null )
    {
        @foreach ( TestingModel m in @Model )
        {
            @if ( @Model != null )
            {
                <h1> @m.name </h1>
            }
        }
    }
</div>

</body>

</html>